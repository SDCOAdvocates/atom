language: none

sudo: required

runtime:
  nodePool: shippable_shared_aarch64
  
build:
 ci:
  - sudo apt-get install -y build-essential git libsecret-1-dev fakeroot npm python rpm libx11-dev libxkbfile-dev
  - ./script/bootstrap
  - ./script/build
  
