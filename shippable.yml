language: none

sudo: required

runtime:
  nodePool: shippable_shared_aarch64
  
build:
 ci:
  - sudo apt-get update && sudo apt-get install -y build-essential fakeroot git libsecret-1-dev libx11-dev libxkbfile-dev npm python rpm
  - ./script/bootstrap
  - ./script/build
  
